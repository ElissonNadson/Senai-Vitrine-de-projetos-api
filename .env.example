# =========================================================================
# CONFIGURAÇÃO DA API - Vitrine de Projetos SENAI-BA
# =========================================================================
# Copie este arquivo para .env e preencha os valores.
#
# AMBIENTES:
#   LOCAL      → npm run start:dev (porta 3000, frontend em localhost:5173)
#   DEV VPS    → docker (porta 3001, https://dev.vitrinesenaifeira.cloud)
#   PROD VPS   → docker (porta 3000, https://vitrinesenaifeira.cloud)
# =========================================================================

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# LOCAL:  http://localhost:3000/auth/google/callback
# DEV:    https://dev.vitrinesenaifeira.cloud/api/auth/google/callback
# PROD:   https://vitrinesenaifeira.cloud/api/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=86400

# PostgreSQL
# LOCAL → conecta no banco DEV da VPS (vitrinesenaifeira.cloud:31982)
# VPS   → conecta no PostgreSQL local (postgres_db:5432 via Docker network)
HOST=vitrinesenaifeira.cloud
DB_PORT=31982
DATABASE=vitrine_senai_dev
USERNAME=api_user
PASSWORD=your-db-password

# Upload
UPLOAD_PATH=./uploads
MAX_FILE_SIZE_MB=10

# Server
PORT=3000
NODE_ENV=development

# Frontend URL (usada em redirecionamentos após OAuth)
# LOCAL: http://localhost:5173
# DEV:   https://dev.vitrinesenaifeira.cloud
# PROD:  https://vitrinesenaifeira.cloud
FRONTEND_URL=http://localhost:5173

# Redis (BullMQ)
# LOCAL: localhost | DEV VPS: vitrine-senai-redis-dev | PROD VPS: vitrine-senai-redis
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS (múltiplas origens separadas por vírgula)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Email / RabbitMQ
# Habilita publicação e consumo de e-mails via RabbitMQ
SEND_EMAIL_NOTIFICATIONS=false

# URL de conexão do RabbitMQ (compose recomendado abaixo)
RABBITMQ_URL=amqp://localhost:5672

# Nome da fila de e-mails
EMAIL_QUEUE_NAME=email_queue

# Configuração SMTP para envio de e-mails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
# "true" para TLS (porta 465 normalmente), "false" para STARTTLS (587)
SMTP_SECURE=false

# Remetente padrão
EMAIL_FROM=nao-responder@example.com
